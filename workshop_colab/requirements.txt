# NEXUS Onboarding Workshop - Python Dependencies
# Install: uv pip install -r requirements.txt
#
# The Fundamental SDK itself is installed separately (it lives in a
# Cloudsmith repository, not on public PyPI). See README.md for the
# install command — it requires CLOUDSMITH_FUNDAMENTAL_TOKEN to be set.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
xgboost>=2.0
ipywidgets>=8.0

# Jupyter environment (needed to run the notebooks)
jupyterlab>=4.0
ipykernel>=6.0
